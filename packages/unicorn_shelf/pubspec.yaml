name: unicorn_shelf
description: HTTP server support (with Shelf) for Unicron.
version: 0.0.1
repository: https://github.com/dart-unicorn/unicorn

environment:
  sdk: ">=2.18.5 <3.0.0"

dependencies:
  unicorn: ^0.0.1

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
